# Base URL of the backend API (no trailing slash), e.g. https://api.example.com
# The frontend fetches `${NEXT_PUBLIC_API_URL}/api/content`.
# Leave empty to run fully on the built-in static fallback content.
NEXT_PUBLIC_API_URL=
